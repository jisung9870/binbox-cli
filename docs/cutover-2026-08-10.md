# Single-user cutover record — 2026-08-10

## Scope

The current machine changed its default `bb` from the legacy checkout symlink
to the checksummed `binbox-cli` release candidate. Legacy binbox, Workbench,
setup, and LazyVim repositories were retained without source changes.

## Installation and rollback

- New executable: `/home/ubuntu/.local/bin/bb`.
- Previous symlink: `/home/ubuntu/.local/bin/.bb.backup.SANJNY` pointing to
  `/home/ubuntu/setup/binbox/bb`.
- Pre-cutover state snapshot:
  `/home/ubuntu/.local/state/bb/cutover-backups/2026-08-10-pre-binbox-cli`.
- Immediate binary rollback: move the installed executable aside and restore
  the recorded symlink backup to `/home/ubuntu/.local/bin/bb`.

Rollback is manual and deliberate. It must not delete the legacy checkout or
the new XDG recovery state.

## Data migration

- Source: `/home/ubuntu/.config/tmux-sessionizer/dirs`.
- Source SHA-256:
  `3ab592c8ffe1771459e4d662fc076dabb12167ff640524ca8a23dfe499d3724a`.
- Imported projects: 8.
- Second apply: 0 imports and 8 already-present records.
- Byte-identical recovery copy:
  `/home/ubuntu/.local/state/bb/migration-backups/sessionizer-3ab592c8ffe17714.dirs`.

The legacy source was not modified. A stale development-only `demo` project
record was removed after its original registry had been included in the
pre-cutover snapshot. Existing journal entries were preserved.

## LazyVim and runtime checks

- Existing link: `/home/ubuntu/.config/nvim -> /home/ubuntu/setup/nvim`.
- Repository identity: `git@github.com:jisung9870/lazyvim-config.git`.
- Revision: `93c236bc8b8753da7ac6a9df34f3a9cdb630053b`.
- Config validation, link validation, and the network-free headless probe
  succeeded without changing the link or repository.
- `git`, `tmux`, `aws`, `terraform`, and Orca were available. `kubectl` was the
  only unavailable optional doctor capability.

The current LazyVim fallback contract is preserved by
`bb tm projects --json`. Human project opening is provided by external
the built-in selector/`tmux`; `bb agents` is intentionally redirected to the Orca ownership
boundary rather than recreating agent lifecycle behavior.
